# BRAND.md — per-repo design context (the local layer)

Drop a `BRAND.md` like this in any repo that uses the `design` skill. The skill reads it first, and **it overrides the skill's defaults**: the portable craft (skill) stays shared; the brand (this file) stays local. This is the "shared skill + per-repo config" split.

Keep it short and concrete — it's injected as context, not documentation.

---

## Subject
- **Product:** <what this is, in one line>
- **Audience:** <who it's for>
- **The page's single job:** <the one thing it must do>
- **Vibe in 3 words:** <e.g. "engineered, calm, precise">

## Palette (3–5 colors, HSL)
- **Primary:** `hsl(...)` — <when to use>
- **Neutral(s):** `hsl(...)`, `hsl(...)`
- **Accent(s):** `hsl(...)`
- Gradients: <allowed? only if explicitly yes>

## Typography
- **Display:** <font> — <weights, when used>
- **Body:** <font>
- **Utility/mono:** <font, if any>
- (If a font is named here, it overrides the skill's banned-font list.)

## Signature / non-negotiables
- <the one element this brand is known for, if any>
- <hard do's>
- <hard don'ts — e.g. "never glassmorphism", "no rounded corners">

## Motion
- <how much, what kind — or "minimal / none">

## Voice (copy)
- <tone, e.g. "plain, technical, no marketing fluff">
- <terms to use / avoid>
